<UserControl x:Class="WpfApp1.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:core="clr-namespace:WpfApp1.Core"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Background="{DynamicResource WindowBackgroundBrush}">
        <Border Background="{DynamicResource PanelBackgroundBrush}" CornerRadius="15" Width="350" Padding="35" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="20" ShadowDepth="4"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Ласкаво просимо" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Логін:" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Height="35" FontSize="14" Padding="5,6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Background="{DynamicResource OverlayBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}"/>

                <TextBlock Text="Пароль:" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,2"/>
                <PasswordBox x:Name="PassBox" Margin="0,0,0,25" Height="35" FontSize="14" Padding="5,6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Background="{DynamicResource OverlayBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}">
                    <b:Interaction.Behaviors>
                        <core:PasswordBindingBehavior Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </b:Interaction.Behaviors>
                </PasswordBox>

                <Button Command="{Binding LoginCommand}" Content="{Binding ButtonText}" Margin="0,0,0,10" Height="45" Background="#4A90E2" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0" Cursor="Hand"/>
                <Button Command="{Binding GuestCommand}" Content="Увійти як гість" Height="40" Background="Transparent" BorderBrush="#4A90E2" BorderThickness="1" Foreground="#4A90E2" FontWeight="SemiBold" Cursor="Hand"/>

                <TextBlock Margin="0,25,0,0" HorizontalAlignment="Center">
                    <Hyperlink Command="{Binding ToggleModeCommand}" Foreground="{DynamicResource SecondaryTextBrush}" TextDecorations="None">
                        <TextBlock Text="{Binding LinkText}"/>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>